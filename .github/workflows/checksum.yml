name: Generate checksum for global_bans/banned.json

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Writing checksum to global_bans/list_checksum
      run: |
        cd global_bans
        md5sum banned.json | tee list_checksum
